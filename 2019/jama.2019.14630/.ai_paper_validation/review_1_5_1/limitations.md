# Limitations and Missing Definitions

These are bounded evidence limitations for the quantitative quality-control review. They do not create additional candidates, determine candidate status, or imply a paper-level conclusion change.

- The supplied package contains five PDFs and no participant-level dataset, analysis code, table-production source, workbook, CSV, DOC, or DOCX. Intended replacement values and production mechanisms cannot be selected from the PDFs alone.
- For C002, participant-level red-wine observations, the quantile convention used to generate the displayed entry, and intended median/quartile values are unavailable.
- For C003-C007, displayed overall arm Ns do not establish row-specific nonmissing baseline denominators or identical baseline records across tables. Participant records, unrounded summaries, baseline handling, calculation output, and rounding or truncation paths are unavailable.
- For C005, the exact SD computation and row-level intervention energy denominator are unavailable.
- For C007, participant-level height and weight, the order of BMI derivation and rounding, and row-specific denominators are unavailable.
- For C008, classification code, implemented inclusive or strict operators, measurement precision at classification, and counts exactly on each threshold are unavailable. The printed operator mismatch does not establish that any reported percentage changes.
- For C001 and C009, the table-production source and author-confirmed intended labels are unavailable. Parallel headings, footnotes, and values support the comparisons but do not identify an authorized correction.
- Statistical sources do not supply contrast-specific standard errors, test statistics, degrees of freedom, covariance structure, variance estimator, multiplicity mapping, or an estimate-to-P calculation rule for the mixed-model results. CI/P checking was therefore limited to source-supported direction, containment, endpoint order, and two-sided display coherence.
- The SAP screening table does not state test, sidedness, model, or variance definitions; its P values were not reverse engineered.
- Some figure coordinates and eFigure labels are graphical rather than printed exact values. EFigure 2 intervention percentages and denominators and eFigure 3 marker/interval coordinates were not invented. This is a reusable-derivative limitation, not an unresolved direct-source page-coverage gap.
- Protocol and SAP quantities are planned or historical definitions and were not treated as observed-result comparators without a matched population, estimand, time, and model.
- Durable artifacts show complete uncapped source and relationship coverage and explicitly exclude legacy selection records as discovery boundaries. Runtime transcripts beyond `agent_execution_manifest.md` are not a scientific evidence source; execution identity, model, effort, and fresh-spawn status are confirmed from that manifest.
- At the time of this audit, final report generation, final manifest closure, token accounting, after-hash registers, HTML rendering, and mechanical validation had not yet occurred. Those are coordinator completion tasks and do not alter the source-grounded candidate evidence.

Every stable candidate remains **Pending Human Adjudication**.
