Apply workflow patch 1.2.2 to this paper package now. This package has workflow-1.0 records and has
already run the endetail pass. Execute the complete endetail-aware recovery workflow in AGENTS.md from
preflight through a passing validator result. Reuse all usable legacy OCR, rendered pages, extracted
text, checker, verifier, critic, candidate, document, and endetail report records; do not restart the
audit from scratch. Harvest completed endetail calculations and explanations before targeted source
confirmation, but do not treat its AI dispositions as authoritative.

You are already authorized for fully autonomous execution: make safe local decisions and do not ask
me whether to execute, continue, enable “turbo,” choose a mode, or approve a recoverable workflow
choice. The Linux target is CPU-only; do not probe for or use a GPU. Reuse old OCR first and use only
targeted CPU OCR if essential. Preserve all source and legacy files. Produce `final_report_1_2_2.md`,
`final_report_1_2_2.html`, and a `PASS` in `patch_1_2_2/patch_validation.json`. Keep the recovered ledger
unbounded, keep the human review queue at no more than 10, and treat every queued candidate as Pending
Human Adjudication rather than making an AI disposition.
