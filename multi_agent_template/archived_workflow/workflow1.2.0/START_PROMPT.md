Run workflow 1.2.0 on this fresh single-paper package now. The directory contains only the main paper
and support files; there are no legacy workflow records to recover. Execute AGENTS.md end to end,
including source preparation, complete result coverage, all checking lanes, both statistical passes,
mechanical rechecking, queue construction, detailed Markdown/HTML reporting, and a passing validator.

You are authorized for fully autonomous execution. Do not ask whether to execute, continue, enable
“turbo,” choose a mode, or approve a recoverable local choice. Use safe deterministic defaults and
parallel specialists. The Linux target is CPU-only: never probe for or use a GPU; use native text and
targeted CPU OCR only. Preserve every source. Keep the candidate ledger unbounded, limit only the
human review queue to 10, and mark every queued item Pending Human Adjudication. Finish only when
`.ai_paper_validation/audit_validation_1_2_0.json` reports `PASS`.
