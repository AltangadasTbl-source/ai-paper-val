Use $ai-validation to audit exactly the paper package in the current working directory.

This is a fresh, isolated worker. Do not inspect sibling paper packages, use prior-paper context,
resume another session, or search the web. Read the shared repository instructions, configuration,
skill, references, specialist-agent definitions, and approved command rules. Make every scientific,
preprocessing, OCR, verification, and reporting decision inside this worker.

Protect this package's context with the configured artifact-first handoff and sharding rules. Complete
specialist results belong in unique files below `audit/`; specialist messages to the coordinator must
remain compact. Complete all recorded shards before synthesis. Sharding never limits total findings.

Preserve source files and write derived files only below ./audit/. Retain every distinct candidate
without a numerical limit. Do not assign severity, validity, acceptance, rejection, or uncertainty.
Complete both statistical passes and generate this package's detailed source-linked Markdown and
standalone `audit/final_report.html` in this run. The package is not complete unless the HTML is
nonempty and `audit/audit_validation.json` reports `PASS`. Finish with a compact JSON response that
conforms to batch/result.schema.json. Use English only.
