schema_version = 1
as_of_utc = "2026-08-18T00:00:00Z"
currency = "USD"
unit_tokens = 1000000
source_url = "UNAVAILABLE_FOR_DYNAMIC_OPENROUTER_ROUTE"
estimate_label = "OPENROUTER_DYNAMIC_ROUTE_TOKEN_COUNTS; PRICE_REQUIRES_RESOLVED_MODEL_RATES"
long_context_threshold_input_tokens = 272000
allow_missing_rates = true
notes = "The configured ~openai/gpt-latest route can resolve to a changing model. Token counts remain auditable, but this template does not fabricate a price. Add dated rates for the exact model/tier/context reported by the runtime before treating any amount as complete."

# Add [[rates]] entries only for an exact resolved model and a verified dated price. Until then,
# calculate_token_cost.py reports exact token totals with INCOMPLETE_PRICE_UNAVAILABLE and leaves the
# complete price blank.
rates = []
