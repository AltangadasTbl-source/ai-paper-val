# Provider authentication remains in the user-level Codex configuration. This project preset pins
# only the reproducible coordinator model and workspace behavior.
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "high"
sandbox_mode = "workspace-write"

[agents]
max_concurrent_threads_per_session = 6
