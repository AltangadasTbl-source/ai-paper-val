# Limitations and Missing Definitions

- C001 cannot be finally reconciled from the supplied package without the raw mRS category counts, unrounded Figure 2 percentages, figure-generation dataset/version, and the percentage-calculation rule.
- C002 cannot be finally relabeled without an authoritative expansion and intended effect-measure name for `aRR`, including whether `a` means adjusted and whether `RR` means risk ratio or rate ratio.
- The review covered supplied local sources only. It did not use external literature, web sources, raw trial data, analysis code, or an author query.
- Reusable text and rendered pages were used as locators and transcription aids; candidate facts were confirmed against direct PDFs. The scanned trial SAP required rendered-page inspection and targeted CPU OCR assistance.
- Token-use values, finished time, final hash comparison, HTML rendering, and validator status are completion steps outside this report-generation response and must be finalized by the coordinator from authoritative runtime records.

These limitations do not create an uncovered direct-source unit, an unchecked relationship, or a basis to suppress either stable candidate. Both candidates remain Pending Human Adjudication.
