Cudkowicz (PI) HEALEY ALS Platform Trial: Master Statistical Analysis Plan                      Version 3.0, 06 Feb 2023


shared parameter analysis. When matched regimen-specific controls are only used, both
the Bayesian shared parameter analysis and the CAFS analysis lead to a null result,
(treatment effect estimate of 1.0, posterior probability DRR<1 of 0.51 and CAFS p-value of -
.3675).




Figure 6.3.2: Example Trial 4. Null Treatment Effect (DRR = 1.00). Differences across regimens in shared control. Top row
shows results from the primary analysis using all shared controls. Bottom row shows results from sensitivity analyses using
                                         only matched regimen-specific controls.




Figure 6.3.3 shows results from a positive simulated trial (30% slowing in disease
progression) where there are no simulated differences across regimens in the shared
control. Within this example, the matched regimen-specific control from the analysis
regimen has a similar rate of progression on ALSFRS-R (red circles) as the controls from
the other regimens in the shared control (pink circles). In particular, the raw mean change
from baseline at 24 weeks in the matched regimen-specific controls is approximately -5.9
and the mean change from baseline at 24 weeks in the other shared controls is
approximately -5.9 and -7.2. The similarities across regimens within the shared control
leads to a fitted ALSFRS-R slope for the matched regimen-specific control that provides a

                                                                                                            Page 59 of 62
