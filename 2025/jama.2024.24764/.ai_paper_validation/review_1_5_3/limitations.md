# Workflow 1.5.3 Limitations and Missing Definitions

The supplied package does not provide participant-level data, executed analysis code, final fitted-model output, later amendments, all result-specific standard errors/degrees of freedom, all covariance and variance-estimation settings, or the unprinted denominator and response-unit definitions needed to resolve every candidate. Numerical coordinates underlying the tipping-point figures are not supplied. No Office document, workbook, or CSV source exists in the package.

These limits prevent final adjudication and some exact reconstructions. They do not negate the direct supplied-source comparisons recorded for C001-C008. All eight stable candidates remain **Pending Human Adjudication**.
