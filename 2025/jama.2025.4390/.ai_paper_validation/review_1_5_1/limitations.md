# Limitations and Missing Definitions

- The supplied package contains four PDFs only; it does not include the locked Figure 3 production dataset, exact arm-specific person-time fields, figure-generation specification, or final layout source needed to select an intended replacement for C001.
- The package does not include medication-level six-month timing records, versioned data-freeze dates, coding history, or figure/table generation code needed to determine which C002 triplet is intended.
- The package does not include the eTable 5 analysis export, participant-level baseline ethnicity coding, data dictionary, table layout source, or row-specific recalculated test output needed to determine the intended C003 cells or any associated inferential result.
- DOC-003 has defective embedded text encoding in places; direct rendering and current-PDF extraction were used for the mapped definitions and methods. This is a production/extraction limitation, not a scientific-coverage gap.
- Some protocol values are explicitly historical or amended plans, and some main-paper values describe external studies. They were not treated as competing final results without a matched population, time, contrast, measure, and analysis definition.
- Reused assets were retained as provenance and locators only. Their stale or incomplete scientific-extraction fitness required fresh direct mapping of all 86 source pages; that mapping is complete, so no scientific-coverage gap remains.
