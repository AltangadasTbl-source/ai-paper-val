# Limitations and Missing Definitions

- Native and layout extraction was readable for every relevant page, but complex table and figure alignment required the freshly rendered page images recorded in the asset inventory.
- Model-adjusted estimates cannot generally be reconstructed from crude displayed counts because the source does not provide the full fitted covariance, working-correlation, and adjustment data.
- Protocol and SAP candidate leads require exact independent checking; mapper leads are not registered candidates.
- No supplied structured raw-data file permits record-level verification, and no external evidence was used.

